# 电商物流配送路径规划系统 设计文档（冻结基线 v2.0）

> 面向对象：本地开发 agent。本文档定义**目标、约束、接口契约与已确认决策**，不规定具体实现细节。实现方式、文件组织、命名、数据结构内部细节由 agent 结合本地环境自行决定。
>
> 课程：数据结构课程设计（2026-2027 学年第 1 学期）
> 题目：第 2 题 电商物流配送路径规划系统
> 技术栈：C++17 / Qt6 Widgets / CMake
> 状态：**设计已明确，可作为开工依据**。配套起步指南见 `docs/设计-交接.md`。

---

## 1. 项目目标

模拟城市物流配送场景，以**有向带权图**构建配送网络，实现订单分配、路径规划、动态调度，并通过 Qt 图形界面可视化网络与路径。

系统需满足题目全部基本要求与扩展要求，并在验收时可应对现场变更需求。

---

## 2. 已确认决策（冻结，不可更改）

| # | 项 | 决策 |
|---|---|---|
| D1 | 语言 | C++17 |
| D2 | GUI | Qt6 Widgets |
| D3 | 核心算法口径 | Dijkstra 逐点最短路累加 + 贪心串联；**不实现全局 TSP** |
| D4 | STL 使用 | **仅核心内容严格禁用 STL；容器与辅助功能积极使用 STL**。边界见 §10.1 |
| D5 | 时间窗口 | 统一口径：**只要送达时刻超出窗口止即标记超时并记录 penalty**，规划与执行不分两套规则。详见 D13 / §4.3 |
| D6 | 到达时间模型 | 绝对时间轴 = 「相对当日 00:00 的分钟数」；车辆以配置的 `depart_time` 发车。详见 §4.3 |
| D7 | 动态订单触发 | GUI 按钮触发；Debug 模式下所有 GUI 操作变为自动模拟；紧急订单优先满足 |
| D8 | 路况模拟 | 随机更新即视为真实数据；仅当前路径上的边耗时增加 ≥ 20% 触发重规划；第一版全量重算 |
| D9 | Qt 架构 | 核心层纯 C++ 只读接口 + GUI 层重建 QGraphicsItem |
| D10 | 配置文件 | 手写 INI 解析器，零第三方依赖 |
| D11 | 多仓库 | 独立配送中心模式，不跨仓调拨 |
| D12 | 车辆数 | 第一版单车辆 |
| D13 | 超时与 penalty | **唯一判据 = 送达时刻 `arrival` 与窗口的关系**。任何阶段的路线求值都调用同一套时间推进函数，因此规划阶段即记录 penalty，重规划后按新路线重算。不再区分「规划只标记 / 执行才计分」 |
| ~~D14~~ | ~~载重与规模的冲突~~ | **已由 D21 修订**：约束对象改为**单趟在车货量**，总需求不再受载重限制；超载触发多趟配送而非判不可行 |
| D15 | 版本控制 | Git，**载体为 GitHub**；全程提交，commit message 语义化 |
| **D16** | 中转站语义 | 中转站是**货物集散点**：可**暂存**货物，供后续趟**二次配发**。载重不足的车辆可反复返回仓库取货并暂存于中转站。详见 §4.1 |
| **D17** | 子网络 | 中转站可嵌套子配送网络，由 `sub_network_id` 标识；用于**界面分组**与**簇划分**，**不是多级图**。详见 §4.1/§5.6 |
| **D18** | 有向性 | 数据集必须含**单向边**。"有向图"须在行为上可区分于无向图。详见 §4.2 |
| **D19** | 路径计数口径 | `路径总数 ≥ 50` 须**同时**满足无向道路对数 ≥ 50 与有向边数 ≥ 50，避免口径歧义。详见 §8.4 |
| **D20** | 多趟 | 载重上限约束**单趟在车货量**；容量不足通过多趟往返解决。车辆每趟**不必**回仓库。详见 §4.5 |
| **D21** | 载重模型修订 | `Σdemand > capacity` **不再判不可行**，改为多趟 + 中转集散。只有"订单不可达"才不可行。详见 §5.5/§5.6 |
| **D22** | 增量式重规划 | 路况变化触发重规划时只重算受影响的路径段；结果须与全量重算一致。详见 §5.7 |

---

## 3. 功能需求

### 3.1 基础功能（必须）

- **B1 图数据结构**：有向带权图，邻接表存储。节点总数 ≥ 30（≥2 仓库、≥15 配送点、≥3 中转站），边 ≥ 50。
- **B2 配置初始化**：从 INI 文件读取节点、边、车辆、订单。
- **B3 图表示输出**：邻接表 / 邻接矩阵文本表示。
- **B4 网络可视化**：GUI 展示节点位置、路径方向、权重标签。
- **B5 动态增删节点/边**：模拟新客户或道路封闭。
- **B6 单车辆路径规划**：从仓库出发，遍历所有待配送点后返回仓库。
- **B7 双策略**：最短距离（距离权重）/ 最低成本（成本权重），均基于 Dijkstra。

### 3.2 扩展功能（必须）

- **E1 路况变化响应**：当前路径上的边耗时增加 ≥ 20% 时自动触发重规划，更新路径高亮。
- **E2 时间窗口**：送达超时即标记 `isLate` 并记录 penalty（口径见 D13）。
- **E3 动态订单插入**：配送过程中新增紧急订单，调整现有路径，优先满足紧急订单。
- **E4 载重约束**：单车辆载重上限硬约束（口径见 D14）。

### 3.3 辅助功能

- **A1 Debug 模式**：开启后所有 GUI 触发操作变为自动模拟。
- **A2 路线信息面板**：路线序列、总距离、总耗时、总成本、到达时间、penalty。
- **A3 日志输出**：重规划、订单插入、超时、路况变化等关键事件。

---

## 4. 领域模型与不变量

本节定义**概念模型与必须遵守的语义规则**，不规定具体存储实现。

### 4.1 节点

- 属性：唯一 ID、类型（仓库 / 配送点 / 中转站）、坐标 (x, y)、名称、子网络编号。
- 不变量：ID 全局唯一；类型决定节点在 GUI 中的角色与颜色。
- **三类节点的语义（D16）**：
  | 类型 | 语义 |
  | --- | --- |
  | **仓库** | 货物**初始存放地**，车辆由此装货出发；满载往返的补给点 |
  | **配送点** | 货物**最终交付地**，带时间窗与需求量 |
  | **中转站** | **货物集散点**：可**暂存**货物，供后续趟**二次配发**；减轻车辆载重上限对总需求的硬性限制 |
- **子网络（D17）**：中转站可**嵌套子配送网络**（如「中转站 A 下属 3 个社区配送点」），
  由 `sub_network_id` 标识：同一编号的节点构成一个子网络，编号 `0` 表示不隶属任何子网络。
  子网络用于**界面分组标识**与**簇划分**（见 §5.6），不是多级图。

### 4.2 边

- 属性：起点 ID、终点 ID、距离 (km)、耗时 (min)、成本 (元)、**基础耗时 `baseTimeMin`**（用于判断「是否增加 ≥20%」与还原）、拥堵标记。
- 不变量：有向；起点终点必须存在；同一对 (from, to) 至多一条边。
- **有向性必须真正体现（D18）**：数据集须包含**单向边**（单行道），
  不能全是双向对。若全部双向，则"有向图"与"无向图"在行为上无从区分，
  与 §5.1「有向带权边」不符，也无法展示 Dijkstra 的有向性。
- **道路计数口径（D19）**：`路径总数 ≥ 50` 按**无向道路对数**与**有向边数**两个口径
  同时满足（见 §8.4），避免验收时因口径解释不同而不达标。

### 4.3 时间模型（统一口径）

**时间轴**：所有时间统一表示为「**相对当日 00:00 的分钟数**」（整数）。配置中的 `HH:MM` 一律按此换算。

**发车时刻**：`t0 = vehicles[].depart_time`（默认 `08:00` → 480 分钟）。仓库不计等待、不计服务时间。

**推进递归**（停靠点 `v` 的前驱为 `u`，起点为仓库故 `u` 的 departure 即发车时刻或上一站离开时刻）：

```
rawArrival(v) = departure(u) + travel_time(u, v)
wait(v)       = max(0, windowStart(v) - rawArrival(v))     // 早到则等待至窗口开启
arrival(v)    = rawArrival(v) + wait(v)
isLate(v)     = arrival(v) > windowEnd(v)
penalty(v)    = isLate(v) ? arrival(v) - windowEnd(v) : 0  // 单位：分钟
departure(v)  = arrival(v) + service_time(v)
```

- **D13 统一口径**：`isLate` / `penalty` 仅由「送达时刻 `arrival` 与该点窗口的关系」决定，**与发生在规划阶段还是执行阶段无关**。规划、重规划、动态插单都调用这同一个推进函数，保证口径一致。
- 早到等待计入总耗时，不计 penalty。
- `service_time` 默认 5 分钟，配置文件可调。
- `windowStart(v)` / `windowEnd(v)` 由该配送点关联的全部订单合并得出（见 §4.4）。

**汇总**：总耗时 = 返回仓库后的 `departure` − `depart_time`；总 penalty = 各停靠点 penalty 之和（回仓库不计窗口）。

### 4.4 订单与停靠点

- 订单属性：ID、配送点 ID、货物量 (kg)、窗口起、窗口止、是否紧急、是否已配送。
- 不变量：窗口起 < 窗口止；配送点 ID 必须存在。
- **停靠点合并规则**：**同一配送点上的多个订单合并为一次停靠**——需求量求和，窗口取各订单窗口的并集区间 `[min(windowStart), max(windowEnd)]`。一个停靠点对应一个节点。
- 无任何未服务订单的配送点不作为停靠点。

### 4.5 车辆

- 属性：ID、起始仓库 ID、载重上限 (kg)、发车时刻 `depart_time`。
- **单车辆**（§5.2 基本要求即「单车辆基础路径规划」），接口预留多车辆扩展。
- **多趟（D20）**：车辆**不再要求一次装完全部货物**。载重上限约束的是
  **单趟在车货量**，而非总需求。容量不足时通过**多趟往返**完成配送（见 §5.5/§5.6）。

### 4.6 路线结果

- 每个停靠点记录：节点 ID、`rawArrival`、等待时间、`arrival`、离开时间、`isLate`、`penalty`、离开时剩余载重。
- **趟（Trip）**：一趟记录其节点序列（含起止节点）、逐节点到达时刻、逐节点是否为配送停靠、
  本趟的停靠点列表、本趟在中转站的**装卸记录**、本趟的距离/耗时/成本。
- 路线汇总记录：**趟列表**、总距离、总耗时、总成本、总 penalty、规划阶段是否可行、不可行原因。
- **中转站暂存状态**：每个中转站的**最终**暂存货量（不变量：规划结束时必须为 **0**）
  与**峰值**暂存货量（供界面展示与守恒验证）。

---

## 5. 核心算法语义

### 5.1 Dijkstra（手写）

- 输入：图、源节点 ID、目标节点 ID、权重类型（距离 / 耗时 / 成本）。
- 输出：路径节点序列 + 总权重；不可达时返回明确标记。
- **必须手写最小堆与松弛逻辑**，不得调用 `std::priority_queue` 或任何 `<algorithm>` 算法。
- 存储容器（如 `std::vector`）可用，见 §10.1。

### 5.2 贪心串联

- 从仓库出发，依次选择「当前点到候选点的最短路权重最小」的候选点作为下一站。
- 候选集合为**未服务订单对应的停靠点**。
- **紧急订单优先**：若存在未服务的紧急订单，仅在紧急订单中选择下一站；否则在所有未服务订单中选择。该逻辑必须**内嵌在贪心选择内部**，不得靠事后排序实现。
- 时间窗口按 §4.3 推进：超时点标记 `isLate` 并记录 penalty，**但仍加入路线**（D13：执行阶段同样是软约束，只记不弃）。
- 最后返回仓库。
- 载重按 D14 在规划前一次性判定；不可达点或超载时返回不可行并给出原因。

### 5.3 动态订单插入

- 将新订单加入订单集合，标记为紧急；若与既有订单落在同一配送点，按 §4.4 合并停靠点。
- 以车辆当前位置为起点、当前时间为初始时间、当前剩余载重为初始载重，对剩余未服务订单重新执行贪心串联。
- 已服务停靠点从待规划集合移除。
- 重算后按 §4.3 重新求值，penalty 以新路线为准。
- 若新订单窗口与现有订单冲突，输出提示。

### 5.4 路况模拟与重规划

- 随机选取一定比例的边（默认 10%，可配置），耗时增加 20%~50% 随机值，标记为拥堵；**直接修改图中边的耗时**（`baseTimeMin` 保留不变）。
- **触发条件**：仅当受影响边位于当前规划路径上，且耗时增加 ≥ 20% 时触发重规划。
- **第一版重规划**：从车辆当前位置出发，对剩余未服务订单全量重算。
- 增量式更新作为扩展点预留接口，不要求实现。

### 5.5 载重约束（D14 / **D21 修订**）

> **本节由 D21 修订**：原 D14 把"载重不足"直接判为**不可行**，与 §4.1 中
> 「中转站是货物集散点、可暂存货物」的语义矛盾——载重不足的车辆完全可以
> **反复返回仓库取货**，把货**暂存在中转站**再**二次配发**。

**约束的正确表述**：

- 约束对象是**单趟的在车货量**：任一时刻 `0 ≤ 车货量 ≤ capacity`。
- **总需求不再受 capacity 限制**。`Σdemand > capacity` 不再判为不可行，
  而是触发**多趟配送**（§5.6）。
- 只有当**存在订单在图上不可达**时，才判定不可行。
- 每站记录**剩余载重**（本趟发车量逐站递减）。
- 中转站暂存满足 `stock[t] ≥ 0`，且**规划结束时必须为 0**（不留残余库存）。

**口径变更的影响**：原「载重约束」人工关卡期望的"不可行"结论**作废**，
改为期望"多趟方案 + 中转站暂存记录"。

---

### 5.6 多趟与中转集散算法（D21）

**触发**：仅当 `Σdemand > capacity` 时进入多趟模式；否则保持**单趟直接送达**
（现有行为与黄金值不变，避免为罕见情形增加常态复杂度）。

**簇划分（数据驱动，D17）**：每个配送点归入它**所属子网络的中转站**
（`sub_network_id` 在数据中标明"某中转站下属哪些配送点"，生成器按最近集散站赋值）。
这使归属是**确定、可核对的数据**，而不是每次规划重算的结果——
需求原文的表述正是"中转站 A **下属** 3 个社区配送点"，属行政归属而非距离计算结果。
`sub_network_id == 0` 的配送点（不隶属任何子网络）归入**仓库簇**、由仓库直接补给。

**趟的定义**：车辆的一段连续行程。起点 = 上一趟的终点（首趟 = 起始仓库），
终点 = 起始仓库或某个中转站。**车辆不必每趟都回仓库**——这正是中转站
减少空驶的机制。

**逐簇处理**（按簇总货量降序）：

```
枢纽 = 仓库:
    批 = 贪心装箱(簇内未送订单, capacity)
    趟 = 车辆移到仓库 → 装批 → 按现有贪心顺序送达 → 回仓库

枢纽 = 中转站 t:
    if stock[t] 足够(≥ 下一个待送订单的货量):
        批 = 从 stock[t] 取货装箱(≤ capacity, ≤ stock[t])
        趟 = 车辆移到 t → 装货(stock[t] -= 批) → 依次送达批内配送点 → 停在 t
    else:
        # 暂存不足，先回仓库补货
        批 = 贪心装箱(簇内未送订单, capacity)
        趟 = 车辆移到仓库 → 装批 → 开到 t → 卸货(stock[t] += 批) → 停在 t
```

**收尾**：全部订单送达后，车辆从当前位置**返回起始仓库**，作为最后一趟。

**必须断言的不变量**：

| # | 不变量 |
| --- | --- |
| 1 | 全部订单被服务（否则才是不可行） |
| 2 | 任一趟任意时刻车货量 ∈ `[0, capacity]` |
| 3 | 中转站暂存**始终 ≥ 0** |
| 4 | 规划结束时**每个中转站暂存 == 0** |
| 5 | `Σdemand ≤ capacity` ⟺ 只有一趟 |
| 6 | 汇总（距离/耗时/成本/penalty）= 各趟累加 |

**关于"效率"的诚实说明（必须写进报告，不得宣称不实收益）**：

在**单车单仓库**模型下，中转站集散**不必然降低总距离**——
把货物预先运到中转站的干线里程，与直接送达的干线里程同阶
（下界均为 `⌈D/C⌉ × 2L`）。它的真实价值是：

1. 使**超载场景从"不可行"变为"可行"**（这是本质改进）；
2. 车辆以中转站为驻点做本地循环，**免除每趟空驶回仓库**，在配送点远离干线时可能更省；
3. 为将来的**多车**模式提供枢纽。

因此报告中评价该机制时应使用「**可行性 + 趟数 + 总权重**」指标，而不是
「比直接送达更省距离」。

### 5.7 增量式重规划（D22）

- **出处**：§5.4 实现提示 2「触发路径重规划时采用增量式更新（仅调整受影响的路径段）」。
- **路线切段**：把当前路线按**停靠点**切成若干 leg（起点→第 1 站、第 1 站→第 2 站、…、
  末站→仓库）。每个 leg 是图上的一段连续行走。
- **受影响判定**：一个 leg 受影响 ⟺ 它走过的边里有一条在路况报告中被标为
  「耗时增幅 ≥ 阈值」。
- **增量做法**：**只对受影响的 leg 重新求最短路**，其余 leg 原样复用；
  **停靠顺序不变**（只调整路径段，不重排服务顺序）。

- **强断言（本阶段的核心测试）**：

  | # | 断言 | 说明 |
  | --- | --- | --- |
  | 1 | 全部停靠点仍被服务，且**顺序与重规划前逐位相同** | 增量不重排 |
  | 2 | **未受影响的 leg 逐位不变** | 这才是"增量"的定义 |
  | 3 | 总耗时 ≤ 重规划前（按变化后的耗时计） | 只可能找到更快的绕行 |
  | 4 | 若**没有任何 leg 受影响**，结果与原路线逐位相同 | 幂等 |
  | 5 | 每段仍可达、仍回到仓库 | 合法性 |

- **为什么不与全量重算比对相等**：全量重算会重新做贪心选点，**停靠顺序可能变化**，
  因此两者本就**不必相等**。把"与全量一致"写成断言是错误的——它会把一个
  正确的增量结果判为失败。全量重算只在**增量不可用**时兜底：
  上一版是多趟方案、或某个 leg 重算后不可达。

- **范围限制**：增量重规划只适用于**单趟**路线；上一版是多趟时直接退回全量重算，
  并在日志中说明。

## 6. 模块边界

核心层与 GUI 层严格分离。核心层不得 `#include` 任何 Qt 头文件，必须能用 g++ 独立编译并跑单元测试。

- **core**：图、节点、边、手写堆、Dijkstra、路径规划、约束检查。
- **io**：INI 解析、配置加载、图表示输出。
- **gui**：主窗口、画布、图元、权重标签、侧栏。
- **tests**：核心层单元测试（不依赖 Qt）。

核心层对外接口应满足 GUI 的只读查询需求（获取节点列表、边列表、当前路线）；GUI 通过调用核心层业务方法（规划、重规划、插入订单、增删节点边）触发计算，再根据结果重建或更新图元。

---

## 7. GUI 语义要求

- 中央画布展示网络：节点按类型着色，边带方向箭头与权重标签。
- 权重标签可在距离 / 耗时 / 成本之间切换（仅更新文本，不重算）。
- 路径高亮：当前规划路线加粗高亮，重规划后更新。
- 工具栏：策略切换、权重标签切换、模拟路况、插入紧急订单、增删节点/边、重新规划、Debug 模式开关。
- 侧栏：订单列表、车辆信息、超时订单、路线信息、日志。
- Debug 模式开启后，原本需要 GUI 触发的操作变为定时自动模拟（路况变化、紧急订单生成、自动重规划），且**必须可随时关闭**。

---

## 8. 配置文件契约

采用手写 INI 解析器，零第三方依赖。

### 8.1 节与字段

- `[general]`：`service_time_min`、`traffic_change_interval_sec`、`traffic_change_ratio`、`traffic_time_increase_min`、`traffic_time_increase_max`、`urgent_order_interval_sec`
- `[nodes]`：每行 `ID, type, x, y, name, sub_network_id`
- `[edges]`：每行 `from, to, distance_km, time_min, cost_yuan`
- `[vehicles]`：每行 `ID, start_node, capacity_kg, depart_time` ← **新增 `depart_time`（HH:MM）**
- `[orders]`：每行 `ID, node_id, demand_kg, window_start, window_end, is_urgent`

### 8.2 示例

```ini
[general]
service_time_min = 5
traffic_change_interval_sec = 30
traffic_change_ratio = 0.1
traffic_time_increase_min = 0.2
traffic_time_increase_max = 0.5
urgent_order_interval_sec = 60

[nodes]
# ID, type, x, y, name, sub_network_id
N01, warehouse, 100, 200, 中央仓库, 0
N02, delivery,  300, 150, 客户A, 0
N03, transit,   250, 300, 中转站B, 1

[edges]
# from, to, distance_km, time_min, cost_yuan
N01, N02, 5.2, 12, 8.0

[vehicles]
# ID, start_node, capacity_kg, depart_time
V01, N01, 800, 08:00

[orders]
# ID, node_id, demand_kg, window_start, window_end, is_urgent
O01, N02, 30, 09:00, 11:00, 0
O03, N05, 20, 10:00, 12:00, 1
```

### 8.3 解析规则

- `#` 开头为注释；`[section]` 分节；逗号分隔字段；空行忽略。
- 时间 `HH:MM` 转为「相对当日 00:00 的分钟数」。
- 解析错误（缺节、字段数不符、类型错误）需给出**带行号的明确报错**，不得静默忽略。

### 8.4 规模约束（`default.ini` 必须满足）

- 节点 ≥ 30：仓库 ≥ 2、配送点 ≥ 15、中转站 ≥ 3
- **路径 ≥ 50，两个口径同时满足（D19）**：
  - **有向边数 ≥ 50**
  - **无向道路对数 ≥ 50**（即"把双向路算作一条"时也不低于 50）
- **必须含单向边（D18）**：至少数条单行道，使有向性可被观察
- **总订单需求 ≤ 车辆载重**：`default.ini` 仍保持单趟可行（保证默认演示简洁）；
  超载场景由 `build/test_capacity.ini`（小容量配置）演示多趟 + 中转集散
- 车辆 `depart_time` 必须早于或等于各订单窗口起点，避免默认全超时
- 每个中转站至少归属 1 个配送点（否则子网络/簇划分无意义，且暂存永远为 0）

---

## 9. 测试要求

测试用例必须覆盖三类：

- **正确运行用例**：默认配置加载、双策略规划、动态增删。
- **导致程序错误的用例**：配置文件缺失、节点 ID 重复、边指向不存在节点、字段数不符/类型错误、载重超限。
- **边界数据用例**：节点数 = 30、边数 = 50、载重刚好等于总需求、送达时刻恰好等于窗口止（`arrival == windowEnd` 不算超时）、及早到等待、空订单列表、不可达点。

核心层测试不依赖 Qt，可用 g++ 直接编译运行。测试数据放 `data/test_cases/`，每份一个 INI 文件。

---

## 10. 约束与禁止事项

**必须：**

- 核心算法与核心数据结构手写。
- 核心层不依赖 Qt。
- 配置文件驱动初始化。
- GUI 可视化网络与路径高亮。
- 全程 Git 提交（GitHub），commit message 清晰。
- 设计留扩展点以应对验收现场改需求。

### 10.1 STL 使用边界（D4 落地细则）

| 范围 | 是否可用 STL | 说明 |
|---|---|---|
| 核心**算法逻辑** | ❌ **严格禁用** | Dijkstra 松弛、最小堆、贪心选择、图遍历的判定逻辑必须手写 |
| 核心**数据结构** | ⚠️ 手写 | `MinHeap` 必须自实现（**不得用 `std::priority_queue`**）；邻接表的遍历/查找逻辑手写，边表**存储**可用 `std::vector` |
| 核心**存储容器** | ✅ 可用 | `std::vector` / `std::string` / `std::map` 等容器可用 |
| `io` / `tests` / `gui` / 日志 / 字符串格式化 | ✅ 积极使用 | 辅助功能不受限，GUI 可用全部 Qt |

**核心层禁止的 STL 算法**（`<algorithm>` / `<numeric>` / `<queue>` 中的算法与优先队列）：`std::sort`、`std::partial_sort`、`std::nth_element`、`std::min_element`、`std::max_element`、`std::lower_bound`、`std::upper_bound`、`std::priority_queue` 等。核心层需要的 `min` / `max` / `swap` 自行内联实现。

**禁止：**

- 核心算法调用 STL 算法（见上表）。
- 核心层 `#include` 或链接任何 Qt 头文件/库。
- 使用第三方 JSON/INI 解析库。
- 同学间抄袭。

---

## 11. 扩展点预留

以下能力第一版不实现，但接口设计需留出空间：

- 多车辆（`RoutePlanner` 接口预留车辆列表参数）
- 跨仓库调拨（预留仓库选择接口）
- 增量式重规划（预留 `replanIncremental` 接口）
- 多趟配送（载重不足时回仓库补货）
- 中转站嵌套子网络（多级图，节点已带 `subNetworkId` 字段）
- 更多权重策略（权重类型枚举可扩展）

**建议**：核心层业务方法的参数尽量用「集合 + 配置」形式，避免硬编码单一车辆、单一策略，便于现场改需求时最小改动。

---

## 12. 开发计划（与课程检查节点对齐）

- 第 1 周：需求分析、设计基线确认。
- 第 2–6 周：核心层（图、堆、Dijkstra、路径规划）+ IO 层。
- **第 7 周：第 1 次进度检查**（需求理解、功能设计、数据结构设计、程序结构原型）。
- 第 8–11 周：GUI 层 + 扩展功能。
- **第 12 周：第 2 次进度检查**（自带数据演示系统初步功能）。
- 第 13–15 周：测试、优化、报告撰写、UML 图。
- **第 16 周：最终验收**（程序、报告电子版、学生证）。

---

## 13. 环境现状（实测）

| 工具 | 状态 |
|---|---|
| g++ | ✅ 16.2.0 |
| cmake | ✅ 4.4.3 |
| git | ✅ 2.55.0 |
| Qt6 | ✅ 6.11.2，**开发头文件/CMake 模块已安装**（`Qt6Widgets` config + 头文件齐全） |
| `gh` CLI | ❌ 未安装（不影响：可直接用 `git` + SSH 推送） |
| GitHub SSH key | ✅ 已生成并将公钥加入账号，`ssh -T git@github.com` 验证通过 |
| git 提交身份 | ✅ `Blorg51688 <dayan51826@outlook.com>`，默认分支 `main` |
| GitHub 远程 | ✅ `origin` → `git@github.com:Blorg51688/logistics-route-planner.git`（**远端仓库尚未创建，首次 push 待用户建仓**） |

**无任何阻塞项**：Qt 可开发，Git 可推送（仅差用户在 GitHub 建空仓库）。

---

## 14. 测试与开发流程约定

### 14.1 期望值必须先由预言机计算（强制）

**规则：TDD 测试中的期望值不得由人工手工推算。**

起因：实现 `Dijkstra` / `RoutePlanner` 期间，人工推算期望值**连续出错三次**（返回段绕经中转站、`W→D2` 最短路非直达、图与测试用错单位），每次都是"测试红了但实现是对的"。这类算术属于高频易错，必须脚本化。

**工具**：`tools/oracle.py`

```bash
python3 tools/oracle.py            # 打印各测试夹具的全部期望值，供抄入 C++ 测试
python3 tools/oracle.py --check    # 自检；已接入 ctest（oracle_selfcheck）
```

**必须方法独立**（否则构成 `tdd-zh` 明令禁止的**同义反复**——期望值由与代码相同的方式重算，凭构造必然通过，永远无法与代码产生分歧）：

| 计算内容 | 被测实现 | 预言机的独立方法 |
|---|---|---|
| 最短路 | C++ Dijkstra + 手写堆 | **穷举所有简单路径取最小**（暴力法） |
| 时间模型 | `RoutePlanner` 代码 | 直接按 §4.3 定义转写 |

预言机只用于**离线生成测试字面量**：不参与构建、不被交付程序引用。C++ 测试中仍是硬编码字面量，运行时保持独立。

### 14.2 测试数据必须由脚本生成并自检（强制）

**规则：交付用的测试数据（`config/default.ini`）不得手工维护，必须由生成器产出且通过自检。**

起因：`default.ini` 原本出自一个写在 `/tmp` 的一次性脚本，脚本未入库。后果是这份数据**无法复现、无法审计、无法参数化重生成**——这正是两个真实数据缺陷（巡游 28 小时、成本与距离线性相关）得以产生的土壤。

**工具**：`tools/generate_dataset.py`

```bash
python3 tools/generate_dataset.py           # 重生成并与已提交文件逐字节比对 + 自检
python3 tools/generate_dataset.py --write   # 自检通过后写回
python3 tools/generate_dataset.py --check   # 只对已提交文件做自检
```

三条保证：

1. **产物可证明**：默认模式下重生成并与 `config/default.ini` **逐字节比对**。数据被手工改动过、或生成参数已变更，都会立刻失败并打印 diff。
2. **生成即自检**：规模（≥30 节点 / ≥2 仓 / ≥15 配送点 / ≥3 中转站 / ≥50 边）、端点存在性、**往返可达性**（有向图"能去"不等于"能回"）、总需求 ≤ 载重、**cost/dist 比值不得恒定**（双策略恒等价的必要条件）。
3. **接入 ctest**：`dataset_selfcheck`。

> 生成器自检是**必要预警**而非充分证明（例如它无法验证"单车辆一天跑得完"）。充分验证在 `integration_tests.cpp` 的数据质量不变量中。

### 14.3 数据质量不变量：用断言取代"人眼看输出"

**规则：对真实数据，除结构断言外还必须断言其"有效性"，尤其是上面两个缺陷的对应性质。**

起因（关键教训）：两个数据缺陷**单元测试全绿**、一路漏到集成阶段，最后是靠**人眼看 `printf` 输出**发现的。也就是说——同样的缺陷重来一遍，测试依然会全绿通过。这是流程缺陷，不是运气问题。

`integration_tests.cpp::checkDataQualityInvariants` 现在守着：

| # | 不变量 | 挡住什么 |
|---|---|---|
| ① | 两种策略必须给出**不同**结果 | 成本与距离线性相关（B7 名存实亡） |
| ② | 各策略在自身目标上不得劣于对方 | "最优"被破坏 |
| ③ | 总耗时 ≤ 12 小时工作日 | 巡游过长、全部订单必然超时 |
| ④ | 超时必须是少数，且只出现在**窗口跨度 ≤ 4 小时**的停靠点上（按跨度判定，不硬编码 ID） | 系统性超时 |
| ⑤ | 两条策略的总距离/总成本**黄金基准** | 非预期漂移 |

**⑤ 的性质必须诚实标注**：黄金值是从**当前行为捕获**的（贪心顺序来自被测实现，oracle 无法独立复现），因此**不用于证明正确性**，只用于检测漂移。有意调整数据/算法时须连同更新。

> **变异验证**：把两个原始缺陷重新注入 `default.ini`，①③④⑤ 均如期失败；恢复后全绿。这条比"测试通过"更能证明断言有效。

### 14.4 独立性的边界（易误用）

| 对象 | 要求 | 理由 |
|---|---|---|
| **算法** | **必须独立** | 否则构成同义反复——期望值由与代码相同的方式重算，凭构造必然通过 |
| **数据** | 应**单一真值源** | 两份定义必然漂移 |

> 已知遗留：单元测试小图目前仍在 C++ 与 `tools/oracle.py` 中各写一遍（双份定义）。风险可控——夹具漂移会让测试**变红**而非静默通过，只是诊断信息不够直观。后续可将小图外置为共享数据文件，两边同源读取。

### 14.5 GUI 的验证方式（离屏渲染 + 目视）

GUI 层不适用前几层的验证手段：渲染错误（箭头画反、标签遮挡、坐标错位）单元测试根本抓不到，
而 Qt widget 点击级自动化（QTest）对课设性价比极低。约定如下：

| 层次 | 方式 |
|---|---|
| 纯逻辑（如「某条边是否在规划路径上」） | 单元测试（已抽出 `isEdgeOnRoute`，被 GUI 高亮与 `needsReplan` 共用） |
| 渲染层 | **`--render` 离屏渲染成 PNG，由 agent 读图目视确认** |
| widget 点击级自动化 | 不做 |

```bash
QT_QPA_PLATFORM=offscreen ./build/app --render out.png --plan distance
QT_QPA_PLATFORM=offscreen ./build/app --render out.png --labels all --weight cost
```

CLI：`--config` / `--weight distance|time|cost` / `--plan distance|cost` / `--labels all|route` /
`--render PATH` / `--width` / `--height`。

> **实际收益**：首版箭头几何直接照搬 Qt `diagramscene` 的 `atan2` 式写法，渲染后发现
> **箭头被画到节点内部而完全看不见** —— 那段几何在该示例里是配合「线段 p1 为终点」的相反
> 约定使用的。已改为「tip + base + 垂直向量」的自证构造。**这个问题只有看图才能发现。**

### 14.6 测试分层

| 层 | 目标 | 依赖 |
|---|---|---|
| `core_tests` / `minheap_tests` / `dijkstra_tests` / `routeplanner_tests` | 用人工小图钉住**行为语义** | 仅 `core` |
| `config_tests` | 配置解析、规模校验、错误输入、边界 | `io` |
| `integration_tests` | 用**真实 `default.ini`** 跑通「配置 → 规划」全链路 | `io` |
| `oracle_selfcheck` | 预言机自身正确性 | python3 |

> 集成层不可省略：`RoutePlanner` 完成后，单元测试全绿，但集成测试一跑就查出**两个真实数据缺陷**（尺度导致 28 小时巡游、成本与距离线性相关导致双策略恒等价）。详见 §15 变更记录中的"默认数据修正"行。

---

## 15. 变更记录

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿 |
| v1.1 | 面向本地 agent 精简；确认 D1–D12 |
| **v2.0** | **冻结基线，可开工**。① D4 落地为 §10.1 显式 STL 边界表；② D6 改为「绝对分钟时间轴 + 车辆 `depart_time`」，关闭「发车时刻未定义」缺口；③ 新增 D13 统一超时/penalty 口径，消除原文「规划只标记 / 执行才计分」的自相矛盾；④ 新增 D14 载重直接判定 + `default.ini` 可行性要求，消除规模与载重的互斥；⑤ §4.4 补停靠点合并规则；⑥ §8 契约新增 `depart_time`；⑦ 新增 D15 GitHub 载体；⑧ Qt 阻塞解除；⑨ GitHub SSH 凭据与提交身份就绪 |
| **v2.0 实现进度** | ① 已落地：项目骨架；`Node`/`Edge`/`LogisticsGraph` 邻接表 **+ 邻接矩阵输出（B3）**；手写 INI 解析器 + `ConfigLoader`；`config/default.ini`；手写 `MinHeap` + `Dijkstra`（三权重维度共用一份实现）；`RoutePlanner`（贪心 + 时间窗 + 载重 + 紧急优先 + `replan`）；E1 路况模拟与重规划触发（`simulateTrafficChange` / `needsReplan`）；E3 动态紧急订单插入（`insertUrgentOrder`）。测试 **624 项**全通过（core 101 + config 201 + minheap 28 + dijkstra 19 + routeplanner 73 + traffic 77 + graphedit 32 + integration 93）。② 待办：报告素材（流程图、UML、测试用例表）与验收准备 |
| **GUI 完成** | ① `MainWindow`：工具栏（策略/权重切换、模拟路况、插入紧急订单、模拟新客户、模拟道路封闭、推进一站、重新规划、手工增删对话框、Debug 开关）+ 侧栏（路线信息/车辆信息/订单列表/超时订单）+ 日志。② B5 按用户要求**两种方式都提供**：一键"模拟"按钮 + 手工增删对话框（指定 ID 增删节点/边）。③ Debug 模式用 `QTimer` 自动模拟路况/插单/推进，且 `debugOn_` 守卫保证随时可关。④ 无头验证：`--render-window` 渲染完整窗口、`--demo N` 触发 N 轮交互后再渲染，截图见 `docs/screenshots/` |
| **人工测试第 1 轮结果：18 关 14 通过 / 4 失败** | 记录见 `docs/人工测试记录.md`。四个失败已全部处理：<br>**① 第 10 关（E3）插单不持久化**：`insertUrgentOrder` 只在局部拼了 `remaining + urgent`，**从未并入 `config_.orders`** → 订单表看不到它，且下一次 `replan()` 用 `remainingOrders()` 重建候选集时上一单被丢掉，连续插单只剩最后一单，「所有紧急订单同级、整体高于普通订单」的性质失效。已改为并入 `config_.orders`；订单号改用 `U001/U002…` 唯一分配（原先用「原订单号+URG」会在重复选中同一单时**撞号**）；窗口从错误的 `0–1440`（全天）改为需求原文举例的**「1 小时内送达」**（`[当前时刻, 当前时刻+60]`）；日志补充货量与送达截止时刻。<br>**② 第 11 关（B5）新客户不入配送**：`addRandomCustomer` 只加节点不加订单，而停靠点只由订单决定 → 新节点永远不会被访问。已改为同时创建订单（编号 `C001…`）并记录日志。<br>**③ 第 15 关（A1）Debug 间隔过长**：配置的 `traffic_change_interval_sec = 30` 是对路况模型本身的描述，但 Debug 模式本质是**演示加速**，30 秒一次让人以为没反应。改为按 **10 倍速**（下限 1.5 秒），并在日志里写清实际间隔与加速说明。<br>**④ 第 17 关（B3）引导不当**：向导原先只显示前几行、要用户自己去 `less` 找文件。改为先列预期、再**整屏分页显示完整输出**，无需手动打开任何文件。 |
| **人工测试第 2 轮：18 关 13 通过 / 5 失败** | 记录见 `docs/人工测试记录.md`（提交 `1ab0470`）。五个失败已全部处理：<br>**① 第 6 关（B4）双向边标签重复**：A→B 与 B→A 画在同一段线上、权重必然相同，两个方向同时高亮时标签完全重叠成"重复显示"。已改为**仅在双向边都可见时抑制其一**（保留节点 ID 字典序较小的一端）；只经过单一方向时仍正常显示，不会把标签整个丢掉。<br>**② 第 9 关（E1）路况只能变拥堵**：需求把实时路况定义为动态属性「拥堵 / 畅通」，而实现只有 `baseTimeMin × (1 + 增幅)` 一条路径。已加 `clearProbability` 参数：畅通时耗时恢复 `baseTimeMin` 且 `congested = false`；`TrafficChange` 增加 `congested` 字段；日志改为「改动 N 条边（新增拥堵 X 条，转为畅通 Y 条）」。畅通不满足"增幅 ≥ 20%"故不触发重规划。<br>**③ 第 10 关（E3）看不到紧急订单**：两层原因——默认数据集**预置了 3 个紧急订单**（而 E3 的语义是"配送过程中动态插入"），以及订单表按插入顺序把新单追加在**表格末尾**、状态列还被挤到可视区外。已：生成器去掉预置紧急订单并重新生成数据（黄金值同步更新，数据质量不变量仍全部成立）；订单表改为**未送达紧急 → 未送达普通 → 已送达**排序，新插单立刻出现在第一行；前 4 列按内容自适应、状态列拉伸，右侧栏加宽到 360。<br>**④ 第 15 关（A1）Debug 插单过频**：原先每个 tick 掷 1/4 概率，3 秒一 tick 意味着平均十几秒就多一单。已改为按配置 `urgent_order_interval_sec` **节流**（该配置项此前根本没被使用），并限制同时待处理紧急订单不超过 2 单；开启时在日志写明节流规则。<br>**⑤ 第 17 关（B3）展示方式**：向导现在**自动用系统默认查看器打开**两个输出文件（等同双击 txt 预览），终端只留节选与备用命令。 |
| **人工测试第 3 轮：18 关 11 通过 / 5 失败 / 2 跳过** | 记录见 `docs/人工测试记录.md`（提交 `36a4e4b`）。五个失败处理如下（其中 2 个是**向导自身的缺陷**，不是程序缺陷）：<br>**① 第 4/5 关：向导期望值过期**（用户判断正确——他看出"只是数据变了"）。我重新生成数据集后，向导里仍写死 `262.2km / 290.8 元`（实际已变为 `237.8 / 251.5`）。**这是引导缺陷**。已新增 `--plan-summary distance|cost` CLI，向导改为**从程序自身取参考值**并展示，数据集再调整也不会过期；精确数值的回归仍由 `integration_tests` 的黄金值断言守住。<br>**② 第 13 关：手工添加边只加单向**。`closeRandomRoad` 与手工"删除边"都是双向操作，唯独"添加边"只加了一个方向，操作不对称。已改为**同时构建双向边**并与删除对称，向导说明同步更新。<br>**③ 第 14 关：推进不刷新画布、且看不到车辆位置**。`onAdvanceStop` 只更新侧栏、不刷新场景；且完全没有车辆位置标记。已新增：`NodeItem::setVehicleHere` 用**品红环**标出车辆所在节点（与红色路径高亮、节点类型色都能区分）；`GraphScene::setVehiclePosition`；推进与重规划后自动 `syncScene()` 刷新，无需手动点"重新规划"。用户还提示：将来支持多车时，行驶中的车应画在当前路段的**起点**，避免把尚未到达的点标成特殊颜色——已记入扩展点。<br>**④ 第 15 关：推进到底后车辆不回仓库**。`onAdvanceStop` 只遍历 `plan_.stops`（配送点），送完最后一站就停住，与 B6「遍历后返回仓库」不符。已给 `RoutePlan` 增加 `returnArrivalMin`（抵达起始仓库的时刻，routeplanner_tests 断言 555），推进在送完后**再走一步返回仓库**并记录日志；`--self-check-actions` 增加第 ④ 项守卫。<br>**⑤ 第 17 关已通过**（自动打开文件预览生效）。<br>另有 2 关被用户主动跳过（载重约束 E4、边界不可行），这两关在前两轮均通过，本轮程序逻辑未触及，且已核对载重约束的期望文本仍准确；并顺手把不可行原因里的 `740.000000kg` 改为 `740kg`。 |
| **人工测试第 4 轮：18 关 14 通过 / 2 失败 / 2 跳过** | 记录见 `docs/人工测试记录.md`（提交 `881fb69`）。两条失败均为**观测性/交互语义**改进：<br>**① 第 8 关（E2）超时订单表信息不足**：原先列为「配送点 / 到达 / penalty / 剩余载重」。用户指出——真正模拟中紧急订单也会超时，只记地点用处不大；而"剩余载重"在这里没有观测价值。已改为「**订单 / 配送点 / 到达 / 窗口 / penalty**」：列出该点上的订单号（同点多单用 `/` 连接），并用**合并窗口**替代剩余载重，超时原因一眼可见（如 D12 窗口 09:00-11:00、实际 15:20 到达）。<br>**② 第 14 关（B6）推进应逐节点而非只跳配送点**：原先只遍历 `plan_.stops`，途经的仓库与中转站不产生任何一步。已给 `RoutePlan` 增加与 `nodes` 一一对应的 `nodeArrivalMin` 与 `nodeIsStop`（routeplanner_tests 断言 `[480,540,555]` / `[false,true,false]`），界面改为**逐个节点推进**：配送点记「送达」，其他节点记「经过」，并自动刷新车辆标记。<br>实现中发现的自身疏漏：`walkPath` 记的是**未经等待的原始到达时刻**（490），而停靠点的实际送达是等窗口开启后的 540 → 已改为以停靠记录为准。<br>另有 2 关被用户主动跳过（载重约束 E4、边界不可行），前几轮均通过；已核对载重约束期望文本仍准确。 |
| **为人工发现的缺陷补自动化守卫** | 新增 `--self-check-actions`（接入 ctest `cli_action_self_check`），自动验证：连续两次插单订单数 +2 且全部紧急配送点整体排在最前；模拟新客户带来订单且新节点被纳入配送。<br>**变异验证**：撤销「并入 config_.orders」→ 3 项失败（含「紧急配送点实际只连续 2 个」，正是用户描述的性质失效）；撤销「新客户创建订单」→ 2 项失败。守卫确有牙齿。<br>**原则**：人工测试发现的缺陷，必须有自动化断言守住，否则同样的回归可以悄悄回来。 |
| **GUI 第三个真实缺陷（由用户实机测试发现）** | **普通启动路径根本不含工具栏与侧栏**：`MainWindow` 只在 `--render-window` 分支里被构造，交互启动走的是另写的一段**裸 `QGraphicsView`** —— 用户运行 `./build/app` 只能看到一块画布，工具栏、四个侧栏、日志、Debug 开关全都不存在。<br>**为什么之前的验证没发现**：我一直用 `--render-window` 截图验证，而那条路径**恰好是唯一正确的路径**——我验证的入口与用户的入口不是同一个。<br>**修复**：把交互启动、窗口截图、demo 动作、UI 探针**合并到同一处构造**，从结构上杜绝两条路径再次分叉；交互启动改为**最大化**（尺寸超过屏幕会把侧栏推到可见区之外，用户会以为面板不存在）；新增 `--ui-probe` 与 `cli_ui_probe` 测试，断言工具栏动作 ≥ 9、停靠面板 ≥ 5。<br>**教训**：验证必须覆盖**用户真正的入口**，而不只是功能能跑通的那个入口。 |
| **GUI 运行暴露并修复的两个真实缺陷** | ① **道路封闭会把网络切断**：封闭 `T02<->D15` 后 D15 成孤岛，一次点击即把演示锁死在"不可行"状态。已改为**不封闭桥**（封闭后两端须仍能互相到达），并加跨 20 种子的连通性测试；想演示"封路导致不可达"请用手工增删。② **当前位置追踪索引错位**：重规划会用全新的 `plan_` 覆盖旧计划，而 `servedCount_` 仍指向旧计划，导致按 `plan_.stops[servedCount_-1]` 回查时刻时取到错误值、penalty 暴涨（实测 5322min → 修复后 423min）。已改为用显式字段保存当前位置与时刻 |
| **GUI 进度** | ① 已落地：`NodeItem`（按类型着色、可拖动、`itemChange` 联动相邻边）、`EdgeItem`（线段 + 箭头 + 权重标签）、`GraphScene`（按配置坐标构建、路径高亮、权重维度切换）、`main.cpp`（CLI + 离屏渲染）。GUI 目标在 CMake 中由 `find_package(Qt6 QUIET)` 守卫，未装 Qt 时自动跳过。② 待办：工具栏、侧栏面板、日志、Debug 模式定时模拟 |
| **模块精化** | `WeightType` 从 `Dijkstra.h` 提到独立的 `core/WeightType.h`：`LogisticsGraph` 需要按维度输出邻接矩阵，但**不应依赖 Dijkstra**。同时把权重取值抽为共享的 `pickWeight`，消除 Dijkstra 与 LogisticsGraph 各写一份的重复 |
| **测试基建补强（本轮）** | 补齐上一轮暴露的 5 个缺口：① 生成脚本入 `tools/generate_dataset.py`，参数化 + 生成即自检 + 与已提交文件逐字节比对，接入 `dataset_selfcheck`；② `integration_tests` 增加数据质量不变量（双策略必须分化 / 各自目标不劣 / 工时上限 / 超时须少数且限紧窗口 / 黄金基准），并以变异验证确实能抓住原先那两个缺陷；③ `traffic_tests` 增加跨 20 种子的不变量测试（并以变异验证）；④ `oracle --check` 覆盖面扩展到贪心、并列、路况计数、拥堵重规划、紧急优先；⑤ `nearlyEqual` 支持容差。测试 546 项、ctest 9/9 |
| **接口精化记录** | 实现 E1 时把 `TrafficChange.cumulativeIncreaseRatio` 更名为 **`increaseRatio`**：每次路况刷新将 `timeMin` 重设为 `baseTimeMin × (1 + 增幅)`，**不叠加**，故"累计"一词名不副实且会让耗时逐次失控。语义（相对 `baseTimeMin` 的增幅）与 §5.4 一致 |
| **TDD 记录** | `MinHeap` / `Dijkstra` / `RoutePlanner` 均按 red→green 逐切片开发，seam 先行约定。**变异测试**证实：Dijkstra 去掉过期条目守卫结果不变（该守卫是性能优化而非正确性必需）；破坏权重选择会被 5 条断言捕获 |
| **默认数据修正（由集成测试发现）** | 集成测试打印出两个**真实的数据缺陷**并已修正：① 距离尺度为 `/10` 时贪心巡游达 1048km、总耗时 28 小时，全部订单必然超时、penalty 高达 8555min → 改为 `/40`（巡游 262km、约 10 小时）；② 生成时 `cost = dist × 1.2` 使**成本与距离完全线性相关**，最低成本策略恒等价于最短距离策略、B7 双策略名存实亡 → 改为按**道路等级**差异化（主干道快但贵、支线慢但便宜）。修正后两策略真正分化：距离 262.2km/成本 290.8 元 vs 距离 288.2km/成本 288.2 元。penalty 仅来自两个刻意收紧的窗口（D05、D12），其余 23 站准时 |

---

*文档版本：v2.0（冻结基线）*
*配套：`docs/设计-交接.md`（工程交接与起步指南）*

---

## 16. 本轮工作清单（依据 2026-09 要求复核 + 中转站语义裁定）

> **引用约定**：本章的「设计 §x」指本文档章节；提到《要求整理》时写「要求整理 §x」，
> 因为两份文档都有 §5.x，不加前缀会产生歧义。
>
> 出入：用户裁定「中转站 = 货物集散点」，并指示「**要求中的核心与扩展提到即将其实现**」。
> 据此把复核发现的问题拆成下列阶段。**只做要求明文提到或由此直接推导的项**；
> 明确未被要求提及的（多车辆、最低耗时策略、在途位置、结果导出、多目标加权等）**不做**。

### P0 数据层（先做，是其余阶段的基础）

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P0.1~~ | ~~加入单向支路~~ **✅ 已完成**：6 条单行道 | D18 / 设计 §4.2 | 单向边 6 条，有向边 106 / 无向对 56 |
| ~~P0.2~~ | ~~无向道路对数 ≥ 50~~ **✅ 已完成**：补 14 对配送点间横向路 | D19 / 设计 §8.4 | 无向对 56 ≥ 50，有向边 106 ≥ 50，自检双口径断言 |
| ~~P0.3~~ | ~~中转站归属配送点~~ **✅ 已完成**：配送点按最近集散站写入 `sub_network_id` | D17 / 设计 §8.4 | T01=9 点 / T02=7 点 / T03=9 点，自检断言每站非空 |
| ~~P0.4~~ | ~~重新生成并更新黄金值~~ **✅ 已完成** | 设计 §14.2 | ctest 13/13；黄金值 157.1/170.8 与 159.9/159.9；不变量全绿 |

### P1 core：多趟与中转集散（本轮主体）

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P1.1~~ | ~~`TransitStock`~~ **✅ 已完成** | D16 | `TransitStock{nodeId,finalKg,peakKg}`、`TransitOp{nodeId,amountKg}` |
| ~~P1.2~~ | ~~`RoutePlan` 改趟列表~~ **✅ 已完成** | D20 / 设计 §4.6 | `trips` 为真源，扁平字段由单一 `flatten()` 派生；单趟黄金值逐位不变（444.100/467.400 精确复原） |
| ~~P1.3~~ | ~~多趟规划~~ **✅ 已完成** | 设计 §5.6 | 按 `sub_network_id` 分簇；无中转站的簇按载重分批；经纬中转站的簇走补给/二次配发状态机 |
| ~~P1.4~~ | ~~移除 `OverCapacity`~~ **✅ 已完成** | D21 / 设计 §5.5 | 改为 `OrderExceedsCapacity`（仅单订单超容量才不可行）；100kg 配置产出 19 趟方案 |
| ~~P1.5~~ | ~~不变量测试~~ **✅ 已完成** | 设计 §14.1 | 合成图 13 项 + 真实数据 14 项；变异验证：出库不扣暂存 / 去掉回场趟 均被抓住 |

### P2 core：增量式重规划

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P2.1~~ | ~~只重算受影响的路径段~~ **✅ 已完成** | D22 / 设计 §5.7 | `replanIncremental`：按停靠点切段，只重算走过被命中边的段；其他段原样复用 |
| ~~P2.2~~ | ~~强断言~~ **✅ 已完成** | 设计 §5.7 | 原「与全量重算一致」是**错的**，已改为 5 条可验证断言；多趟情形退回全量重算 |

### P3 子网络

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P3.1~~ | ~~`sub_network_id` 接上行为~~ **✅ 已完成** | D17 | ①簇划分依据（P1）②界面新增「中转站/集散」面板显示子网络编号与下属配送点数；`--ui-probe` 断言其非 0 |

### P4 UI

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P4.1~~ | ~~中转站暂存货量显示~~ **✅ 已完成（面板）** | 用户裁定 | 「中转站/集散」面板列出峰值与当前暂存；默认数据全 0，100kg 配置为 100/98/94 且终值全 0。**节点旁数值未做**（面板已满足需求） |
| ~~P4.2~~ | ~~多趟路线呈现~~ **✅ 已完成** | D20 | 路线面板列各趟（起终点/节点数/距离）与装卸记录；车辆推进沿 `nodeTripIndex` 跨趟连续，并把到达中转站翻译成「入库暂存/取货二次配发」 |
| ~~P4.3~~ | ~~路线面板增加趟数与暂存~~ **✅ 已完成** | 设计 §4.6 | 面板显示「共 N 趟」+ 各趟明细；中转站暂存在专门面板 |

### P5 测试与人工向导

| # | 事项 | 依据 | 验收口径 |
| --- | --- | --- | --- |
| ~~P5.1~~ | ~~改写载重关卡~~ **✅ 已完成（待人工执行）** | D21 | 第 16 关改为「多趟配送与中转集散」：期望不再有"不可行"，改为 N 趟 + 各趟装卸 + 中转站面板峰值非 0/终值 0 |
| ~~P5.2~~ | ~~新增关卡~~ **✅ 已完成（待人工执行）** | D16/D18/D22 | 新增第 17 关「单向路与有向性」、第 18 关「增量式重规划」；总关卡 18 -> 20 |
| ~~P5.3~~ | ~~黄金值同步~~ **✅ 已完成** | 设计 §14.2 | ctest 13/13；向导的数值期望改由 `--plan-summary` 现场取值，不写死 |

### P6 人工测试第 6 轮追加项

| # | 事项 | 依据 |
| --- | --- | --- |
| ~~P6.1~~ | ~~新增第三种「最低耗时」策略~~ **✅ 已完成** | 用户第 6 轮第 4 关要求（原本列为"不做"，用户要求后实现） |
| ~~P6.2~~ | ~~邻接表/邻接矩阵改为**真正的表格**~~ **✅ 已完成**：工具栏「图表示…」打开对话框，两张表 | 第 19 关 |
| ~~P6.3~~ | ~~只高亮**尚未走完**的路段~~ **✅ 已完成** | 第 15 关 |
| ~~P6.4~~ | ~~「当前暂存」随推进更新（而非规划终值，终值恒为 0 没有观测价值）~~ **✅ 已完成** | 第 16 关 |
| ~~P6.5~~ | ~~Debug 自动模拟加入道路封闭（低概率，每 20 tick 至多一次）~~ **✅ 已完成** | 第 12 关 |
| ~~P6.6~~ | ~~向导第 17/18 关没启动程序（第 16 关末尾已 stop_app）~~ **✅ 已完成** | 第 18 关 |
| ~~P6.7~~ | ~~`--plan-summary` 补超时站点与趟数；向导不再写死具体站点与截图~~ **✅ 已完成** | 第 3、8 关 |

### P7 默认载重定档：低载重成为常规数据（用户选择方案 C）

用户在复核第 6 轮时提出：低载重（多趟 + 中转集散）应当成为**常规测试数据**，
以体现"低容量车反复往返、经中转站暂存后二次配发"这一机制的价值；
但同时必须保住既有质量不变量。据此做了参数扫描后定档：

**定档结果：`SCALE = 64.0`、`CAPACITY_KG = 200`、`BRANCH_TIME_FACTOR = 2.4`、`BRANCH_COST_FACTOR = 1.2`**

（先定 SCALE=60 / BRANCH_TIME=1.8，但那时**最低耗时策略与最短距离策略输出完全相同**；
调 BRANCH_TIME 让二者分化后，成本策略又会慢到让宽窗口点 D16 晚 3 分钟。
于是把 SCALE 与支线系数一起扫描，四条约束同时满足的组合有 12 个，取其中最接近原 SCALE 的一组。）

| 约束 | 结果 |
| --- | --- |
| 载重 < 最小簇货量（220kg） | 200 < 220 ✓ 三个中转站**全部参与**集散（峰值 190/178/196kg，终值全 0） |
| 载重 > 最大单订单（42kg） | ✓ 任一订单都装得下 |
| 各策略总耗时 ≤ 720min | 距离 515.8 / 成本 **614.8** / 耗时 515.5 ✓ |
| 超时仅限紧窗口点 | 只剩 **D05（窗口 09:00-10:00）与 D12（09:00-11:00）**，两个最短窗口点 ✓ |
| 三策略**互异**且各自占优 | 距离 **198.2km**（最小）/ 成本 **267.0 元**（最小）/ 耗时 **515.5min**（最小）✓ |

**为什么调 `SCALE` 而不是别的**：要让中转站参与，载重必须低于最小簇货量（220kg），
就必然产生十几趟行程；而行程一多，**成本策略**（偏好 1.0 元/km 但 1.8 min/km 的
慢速支路）的总耗时会被放大十几倍、突破工作日。缩短坐标→公里换算（SCALE 40→60）
等比例压缩全部里程与耗时，同时**不改变三种策略的相对优劣**，是唯一能同时满足
"载重够低"与"仍在工作日内"的旋钮。

### P8 多趟模式下的紧急订单优先（真实回归）

引入多趟后，`insertUrgentOrder` 的"紧急订单优先"失效：算法按**簇**逐个处理，
紧急订单若落在靠后的簇里就会被排到最后，违反 E3「优先满足紧急订单」。

先尝试的修法（让含紧急订单的簇优先处理）**不够**：若两个簇各含紧急单，
它们仍会被各自簇的行程隔开，扁平停靠序列里不连续。

最终修法：**紧急订单单独成趟、最先执行**——先把全部未服务紧急订单按载重分批，
每批一趟直接从仓库送达后返回，再按原逻辑处理其余订单的簇。
这既满足「优先满足紧急订单」，也符合现实（紧急单应立即派车）。

### P9 向导文案的引号截断检查

本文件反复出现把中文引号误写成英文 `"` 的情况（如 `want "**不显示"不可行"**"`），
导致传给函数的实参被静默截断。**`bash -n` 查不出这类错误**（语法合法、语义错），
本项目已踩过两次。已新增 `tools/check_wizard_text.py` 并接入 ctest（`wizard_text_lint`），
检查 `want/dim/say/todo/head1/stage` 这些文案行的引号数量。

### P10 死代码审计（由子 agent 独立完成，我逐条复核后处置）

用户建议用子 agent 做独立校验，理由是"它的数据自己在工作区找，不会因为上下文想当然"。
第二个子 agent 对 `src/` 全部代码 + 配置字段 + 命令行选项做了**死代码/接不上功能**的审计，
逐符号统计声明/写入/读取点，并用 `g++ -Wall -Wextra -Wunused` 实测复核。处置如下：

| 发现 | 我的复核 | 处置 |
| --- | --- | --- |
| **`Trip::nodeTripIndex` 既不写也不读** | ✅ 属实，**是我自己的编辑 bug**：加字段时 `replace(..., 1)` 命中了 `Trip` 里先出现的 `nodeIsStop`，把字段插进了错误的 struct，`RoutePlan` 里另有一份才是活的 | **删除** |
| 7 个死访问器（`GraphScene::{allLabelsVisible,edgeItemCount,nodeItemCount}`、`EdgeItem::{labelVisible,edge}`、`NodeItem::node`、`IniFile::sections`） | ✅ 全仓 0 调用 | **删除** |
| `showInteractive` 的 `width`/`height` 局部变量写后未读、`preferredHeight` 无效果 | ✅ 属实（编译器 `-Wunused-variable` 实测报警） | **删除无效参数与死变量**，交互窗口始终最大化；`--help` 写明 `--width/--height` 只对渲染路径生效 |
| `onDebugToggled` 里 `ticksPerUrgent` 写后未读 | ✅ 属实（与 `onDebugTick` 中的同名有用变量重复） | **删除** |
| `expect(scene_ != nullptr, "场景已建立")` 是**恒真断言**，注释却称校验车辆标记 | ✅ 属实——这正是本项目一直警惕的"空跑断言" | **改为真断言**：新增 `GraphScene::vehiclePosition()`（有使用者）并断言画布标记 == 当前位置；变异验证退出码 1 |
| `--plan` / `--labels` 只在 `--render` 分支被读，其他路径静默忽略 | ✅ 属实 | 在非 `--render` 路径上**直接报错**并说明原因 |
| `--weight` 只在 `--render` 分支被读 | ❌ **审计有误**：`--dump-graph matrix` 也读它（`main.cpp:224`） | 无需处置（已核实） |
| `Stop::{rawArrivalMin,waitMin,departureMin,remainingLoadKg}`、`TransitStock::finalKg`、`Trip::totalTimeMin`、`RoutePlan::returnArrivalMin` 只有测试在读 | ✅ 属实 | **接上界面**而非删除：新增「**停靠明细**」面板展示 原始到达/等待/送达/离开/剩余载重（这些是设计 §4.6 明确要求记录的字段，报告也要求呈现"到达时间"）；`returnArrivalMin` 的过期注释已改正 |
| `MinHeap::size()` 仅测试调用 | ✅ 属实 | **保留**——它是容器应有的 API 且确有使用者（测试），不属于死代码 |
| 配置字段、核心函数入口 | 未发现"解析了却零读取"或"无入口"的 | 无需处置 |

**子 agent 的价值在这次体现得很直接**：`Trip::nodeTripIndex` 这个重复成员是我自己写出来的，
而我读过那段代码多次都没发现；恒真断言也是我自己写下并"以为它在检查东西"的。
但它的报告**也有一处不准确**（`--weight`），所以我仍然逐条核实后才动手。

### P11 人工测试第 7 轮的处理

**用户反馈的改进项（已实现）**

| 项 | 内容 |
| --- | --- |
| 下拉顺序统一 | 「规划策略」原为 距离/成本/耗时，与「权重标签」的距离/耗时/成本不一致；已统一为 **距离 → 耗时 → 成本** |
| **当前载重** | 车辆面板新增「**当前载重**」（随送达递减），与「本趟装载」（本趟出发时的量）、「剩余待送」（全部未送达之和）三者分列，互不混淆 |
| **峰值暂存实时化** | 峰值不再是规划时算好的定值，而是**随推进实时刷新**——只有真的存进去且当前量超过历史峰值时才变大（`stockTrace`）。「当前暂存」同理随推进变化 |
| Debug 速度档位 | 新增工具栏「Debug 速度」下拉：**展示模式（2 秒/步）** 与 **快速预览（1 秒/步）**，切换即时生效 |
| **预期同步纳入自动核实** | 新增 `tools/check_wizard_claims.py` 并接入 ctest：把向导里**写死的规模/结构数字**与 `config/default.ini` 实际值对照（节点数、各类型数、单向边数、载重上限、子网络配送点数），并断言"载重 < 最小簇货量"与"载重 > 最大单订单"。此前"预期过期"已在第 3、6、7 轮反复发生，现由脚本每次守住 |

**用户要求向导瘦身（已执行）：20 关 → 13 关**

用户原话："关卡过度耗时过大，多次证明无错误的内容不应该再进行测试"。
据此把已并入常规流程或 GUI 已有更直观入口的关卡合并/移除：

| 原关卡 | 处置 |
| --- | --- |
| 多趟配送与中转集散（E4） | **并入**第 4 关——集散已是默认行为，不再单列 |
| 单向路与有向性（D18） | **并入**第 3 关（与网络图一起看） |
| 增量式重规划（D22） | **并入**第 8 关（与模拟路况一起看日志） |
| 图表示输出（B3） | **并入**第 3 关，且改为看 GUI 里的**表格对话框**（用户：已有 GUI 图表，无需再看文件） |
| 模拟新客户 / 道路封闭 / 手工增删 | **合并**为第 10 关（三件同类事一次做完） |
| 三种策略 + 权重标签 | **合并**为第 5 关 |

新结构 13 关：构建自动检查 → 界面布局 → 网络图/有向性/图表示 → 规划与集散 →
三策略与权重 → 拖动联动 → 超时 → 路况与增量重规划 → 紧急订单 → 动态增删 →
推进与车辆/暂存随动 → Debug → 边界不可行。

**关于"中转站提高效率"的再次核查（未实现不存在的优化）**

用户提出：车辆应"满载进入中转站""从仓库出发时顺带补充中转站"，避免空驶，
从而提高效率。我做了量化核查后**没有实现这一改动**，因为算下来它不成立：

货量从仓库跨到某簇所在区域，是任何方案都躲不掉的干线运输。每跨一次最多带走
一个载重上限 C，因此每簇的干线跨越次数下界是 `ceil(簇货量 / C)`：

    簇1 262kg → 至少 2 次；簇2 220kg → 至少 2 次；簇3 258kg → 至少 2 次

**直接送达**恰好达到这个下界；**经中转站**同样是这个下界，却额外产生
「中转站 → 配送点」的短途往返。所以中转站**不减少干线跨越次数，也就不降低总距离**。

它的真实价值仍是设计 §5.6 早已写明的三条：使超载场景可行、车辆以站为驻点
免除每趟空驶回仓库、为将来多车提供枢纽。**按用户先前的裁定，报告中不得宣称
不实收益**，故此处保持原样，并把核算过程留档。

**第 20 关"程序未能启动"**：实测无参数启动可正常存活，**无法复现**。
已加固 `launch_app`：程序输出落盘到 `/tmp/wizard_app.log`，轮询等待延长，
失败时直接把该日志打印出来——下次若再出现，即可看到真实原因。

### P12 第 9 轮裁定：中转站**不强制参与路由**（用户原则）

用户裁定（原文）：

> 中转站名字保持不变，并不是强行要经过中转站如果本来直达更快就应该去直达，
> 中转站只应该在"马上就要带着货物返回仓库"时放下这部分马上要被浪费的载荷……
> **一切决策都不应该为了满足某种策略的前提条件而去实际执行更差的策略。**

据此把 `multiTripPlan` 的中转站判定改为：

```cpp
const bool useStation = !hub.empty() && clusterTotal > vehicle.capacityKg + 1e-9
                        && stock[hub] > 1e-9;   // 只有站内**确实有存货**才考虑
```

中转站**初始无存货**，因此默认数据一律走直达分批。改动前后：

| 策略 | 改前（每簇必经站） | 改后（直达分批） |
| --- | --- | --- |
| 距离 | 198.2 km / 13 趟 / 277.2 元 / penalty 323 | **178.2 km / 6 趟 / 244.2 元 / penalty 256** |
| 成本 | 209.1 km / 267.0 元 / penalty 382 | **186.3 km / 235.9 元 / penalty 307** |
| 耗时 | 200.5 km / 281.6 元 / penalty 323 | **183.2 km / 255.2 元 / penalty 311** |

**三项策略、四项指标全面改善，趟数减半。**

`replan` 新增 `initialStock`（各站期初存货，默认为空）作为一等输入：
站里确实有货时，规划才会把该站当前置仓库使用。这一路径已有测试覆盖
（`testStationUsedOnlyWhenStockExists`：期初 0 完全绕开；期初有货则启用）。

对应地，三条旧断言（"中转站必须被使用""峰值合计 > 0"）已被**改写为守住新原则**：
**期初无存货时中转站不得参与路由**——一旦有人把"每簇必经站"加回来，它们立刻失败。

### P13 尚待实现：库存的生产者（顺路寄存）

现在 `initialStock` 已能作为输入，但**没有任何东西去填充它**——
即用户所说的"车辆即将带货运回仓库时，把马上要浪费的载荷顺路卸在中转站"。

要落地需要一个前提：**重规划时必须知道车上此刻载着什么**。
当前 `replan` 完全按 `remainingOrders()` 从零重算，**不知道车辆在途载货**，
因此中途重规划（紧急订单 / 路况 / 封路）会让车先空跑回仓库，把在途货物白带一趟。
这本身就是一个独立缺陷（见 P13 待办）。

已具备的素材：`MainWindow::currentLoadKg()` 能从"当前趟 + 推进位置"反推在途货量，
所以不必新增一份状态即可着手。

### P13 顺路寄存（前置储存点机制）——已实现

用户第 10 轮确认按**安全版**实现：只卸在**回程路径上的站**，且**必须是该站所服务簇的货**。

**关键简化**：寄存发生在规划窗口**之前**，因此不需要在任何行程里记一笔装卸，
直接体现为站里的**期初存货**即可。整个机制因此只有几十行。

`replan` 新增两个可选参数：

- `initialStock`：各站期初存货（前置储存点）
- `onboard`：车辆**此刻已载在车上**的货（`OnboardItem{nodeId, kg}`）

处理顺序（`multiTripPlan`）：

1. **先空跑一版**，得出"本次回仓库**之前**会服务哪些停靠点"；
2. 在途货里**没被这一版送掉**的部分，若其所属簇的中转站满足
   `dist(出发地,站) + dist(站,仓库) <= dist(出发地,仓库)`（即**站在回程路径上**），
   就把它并入期初存货 —— **零绕路**；不满足则原样带回去；
3. 算两版（不许用站 / 允许用站），**取更优者**，用站版 penalty 更差则一票否决。

**必须"先空跑一版"的原因**：把**全部**在途货都寄存是错的——车马上就要送掉的那些
不该卸下来。所以要按"本次回程前是否来得及送"来筛。

实测（`testOnboardSurplusIsBankedEnRoute`，载重 50kg、订单各 20kg、车在 D1 载着 D2 的货）：

```
第1趟: D1 T W                       <- 回仓库路上经过 T，顺路寄存 20kg（零绕路）
第2趟: W T D1 D2 D1 D3 D4 D3 D1 T W
T 峰值 20.0kg  期末 20.0kg          <- 存货留下，供后续使用
```

**触发条件**：不是常规直送——常规直送时车**空车**回仓库，没有可寄存的货。
真正触发的是**途中重规划**：车正在簇内执行某趟、车上载着尚未送达的货，
而新规划要求它先回仓库（例如插入远端紧急订单）。此时那部分货才"来不及送"。

**"积少成多"的回路**：GUI 用 `stationStock_` 保存最近一次规划的期末存货，
下一次重规划时作为 `initialStock` 传回（三处规划出口都会回写）。
这样寄存的货不会凭空消失，会跨重规划累积，直到被某次规划当作前置仓库用掉。

### P14 第 11 轮：两处界面呈现修正 + 向导再瘦身

**用户反馈 1：停靠明细展示混乱、等待/载货数据看起来不对。**
逐值核对后确认**数据本身无误**（D01 等待 55 分 = 窗口 09:00 − 原始到达 08:05；
送后余载 160 = 首趟装载 190 − D01 需求 30）。真正的毛病是**呈现**：
明细是**跨趟拉平**的，`剩余载重` 会从 0 跳回几十公斤、再跳到一百多，
没有趟号时看起来就像数据错了。处置：新增「**趟**」列，并给列名补上单位
（`等待(分)`、`送后余载(kg)`）。并加守卫：`--ui-probe` 断言每行首列都是「第 N」。

**用户反馈 2：完整路径上方的「第 X 趟」全是 `W01 → W01`。**
原因是该行渲染的是 `trip.nodes.front() → trip.endNodeId`；直达分批下每趟都从
仓库出发、回仓库，这两个端点**已无信息量**。处置：改为显示本趟**服务了哪些配送点**：

```
第 1 趟：7 个配送点  D01 → D04  28.2km  装载 190kg
```

**用户反馈 3：再精简向导，把"只是看看"的关卡合并。**
13 关 → **8 关**。原来"认清布局""看网络图""看规划结果""看超时表""看三策略"
是**五关纯粹的看**，熟练测试者每关都要清屏、回车、再切回程序，成本全在切换
而不在验证。合并后：

| 新关卡 | 合并了原来的 |
| --- | --- |
| 1 构建与自动检查 | 1 |
| 2 启动、界面布局、网络图与图表示 | 2 + 3 |
| 3 规划结果：路线/车辆/中转站/明细/三策略/超时 | 4 + 5 + 7 |
| 4 画布交互与手工增删健壮性 | 6 + 10(部分) |
| 5 动态事件：路况/增量重规划/紧急订单/新客户/封路 | 8 + 9 + 10(部分) |
| 6 推进一站：车辆位置、当前载重与暂存随动 | 11 |
| 7 Debug 模式 | 12 |
| 8 边界：制造不可行场景 | 13 |

每个"要不要动手"的环节仍是独立一关，不打乱节奏；纯"看"的部分全部并进相邻关。

**顺带消灭一处新的硬编码**：向导原本写死"停靠明细第一行应为 第 1 | D01 | 08:05 | ..."，
这又是一处会随数据过期的东西。已改为**现场跑 `--ui-probe` 取值展示**。

### P15 第 12 轮：修"趟号塌成 1"的显示 bug + Debug 速度卡片删除

**用户反馈（第 7 关）**：Debug 过程中停靠明细的「趟」与「等待」列显示错误
——"一直显示的趟1"，要求"修复时自己先推进几站，在动态中持续保持正确"。

**根因（我上一轮没修对的地方）**：GUI 把**整条剩余路线切成了一趟**（`restTrip`）
塞给增量式重规划。于是走增量路径后 `plan_.trips.size() == 1`，界面上的
「第 N 趟」全塌成「第 1 趟」、「共 N 趟」变成「共 1 趟」、车辆面板变成「第 1 / 1 趟」。

> 教训：上一轮我加的是"趟号列"，看着把问题修了，但**根因在更上游**——
> 剩余路线的切分方式。只在显示层打补丁，数据层已经塌了。

**修法**：把"切分尚未走完的部分"抽成 core 的 `sliceRemainder(plan, fromIndex)`，
**按趟切分、保留真实趟结构**。多趟时增量算法本就会退回全量重算（日志已说明），
此时结构自然正确；单趟时增量路径照常生效。

**守卫（三层，都经过反向验证）**：
1. `integration_tests`：走到第 3 个停靠点后切分，断言剩余部分**仍分多趟**
   （变异成"压成一趟"→ 报"实际 1 趟"失败）
2. `--ui-probe`：断言停靠明细的**趟号列真的出现多种趟号**（共 6 趟时至少 2 种）
   （变异成全塌成 1 → 报"共 6 趟，但趟号只有 1 种"退出码 1）——
   这是直接守**用户看到的那个界面**
3. 向导第 6 关：让测试者推进后回看停靠明细，核对趟号仍是 1、2、3…

**Debug 速度卡片**：按用户要求**只保留 1 秒档**、删除速度选择下拉与标签，
工具栏动作从 17 项降到 15 项。

### 明确**不做**的项（要求未提及）### P10 死代码审计（由子 agent 独立完成，我逐条复核后处置）

用户建议用子 agent 做独立校验，理由是"它的数据自己在工作区找，不会因为上下文想当然"。
第二个子 agent 对 `src/` 全部代码 + 配置字段 + 命令行选项做了**死代码/接不上功能**的审计，
逐符号统计声明/写入/读取点，并用 `g++ -Wall -Wextra -Wunused` 实测复核。处置如下：

| 发现 | 我的复核 | 处置 |
| --- | --- | --- |
| **`Trip::nodeTripIndex` 既不写也不读** | ✅ 属实，**是我自己的编辑 bug**：加字段时 `replace(..., 1)` 命中了 `Trip` 里先出现的 `nodeIsStop`，把字段插进了错误的 struct，`RoutePlan` 里另有一份才是活的 | **删除** |
| 7 个死访问器（`GraphScene::{allLabelsVisible,edgeItemCount,nodeItemCount}`、`EdgeItem::{labelVisible,edge}`、`NodeItem::node`、`IniFile::sections`） | ✅ 全仓 0 调用 | **删除** |
| `showInteractive` 的 `width`/`height` 局部变量写后未读、`preferredHeight` 无效果 | ✅ 属实（编译器 `-Wunused-variable` 实测报警） | **删除无效参数与死变量**，交互窗口始终最大化；`--help` 写明 `--width/--height` 只对渲染路径生效 |
| `onDebugToggled` 里 `ticksPerUrgent` 写后未读 | ✅ 属实（与 `onDebugTick` 中的同名有用变量重复） | **删除** |
| `expect(scene_ != nullptr, "场景已建立")` 是**恒真断言**，注释却称校验车辆标记 | ✅ 属实——这正是本项目一直警惕的"空跑断言" | **改为真断言**：新增 `GraphScene::vehiclePosition()`（有使用者）并断言画布标记 == 当前位置；变异验证退出码 1 |
| `--plan` / `--labels` 只在 `--render` 分支被读，其他路径静默忽略 | ✅ 属实 | 在非 `--render` 路径上**直接报错**并说明原因 |
| `--weight` 只在 `--render` 分支被读 | ❌ **审计有误**：`--dump-graph matrix` 也读它（`main.cpp:224`） | 无需处置（已核实） |
| `Stop::{rawArrivalMin,waitMin,departureMin,remainingLoadKg}`、`TransitStock::finalKg`、`Trip::totalTimeMin`、`RoutePlan::returnArrivalMin` 只有测试在读 | ✅ 属实 | **接上界面**而非删除：新增「**停靠明细**」面板展示 原始到达/等待/送达/离开/剩余载重（这些是设计 §4.6 明确要求记录的字段，报告也要求呈现"到达时间"）；`returnArrivalMin` 的过期注释已改正 |
| `MinHeap::size()` 仅测试调用 | ✅ 属实 | **保留**——它是容器应有的 API 且确有使用者（测试），不属于死代码 |
| 配置字段、核心函数入口 | 未发现"解析了却零读取"或"无入口"的 | 无需处置 |

**子 agent 的价值在这次体现得很直接**：`Trip::nodeTripIndex` 这个重复成员是我自己写出来的，
而我读过那段代码多次都没发现；恒真断言也是我自己写下并"以为它在检查东西"的。
但它的报告**也有一处不准确**（`--weight`），所以我仍然逐条核实后才动手。

### P11 人工测试第 7 轮的处理

**用户反馈的改进项（已实现）**

| 项 | 内容 |
| --- | --- |
| 下拉顺序统一 | 「规划策略」原为 距离/成本/耗时，与「权重标签」的距离/耗时/成本不一致；已统一为 **距离 → 耗时 → 成本** |
| **当前载重** | 车辆面板新增「**当前载重**」（随送达递减），与「本趟装载」（本趟出发时的量）、「剩余待送」（全部未送达之和）三者分列，互不混淆 |
| **峰值暂存实时化** | 峰值不再是规划时算好的定值，而是**随推进实时刷新**——只有真的存进去且当前量超过历史峰值时才变大（`stockTrace`）。「当前暂存」同理随推进变化 |
| Debug 速度档位 | 新增工具栏「Debug 速度」下拉：**展示模式（2 秒/步）** 与 **快速预览（1 秒/步）**，切换即时生效 |
| **预期同步纳入自动核实** | 新增 `tools/check_wizard_claims.py` 并接入 ctest：把向导里**写死的规模/结构数字**与 `config/default.ini` 实际值对照（节点数、各类型数、单向边数、载重上限、子网络配送点数），并断言"载重 < 最小簇货量"与"载重 > 最大单订单"。此前"预期过期"已在第 3、6、7 轮反复发生，现由脚本每次守住 |

**用户要求向导瘦身（已执行）：20 关 → 13 关**

用户原话："关卡过度耗时过大，多次证明无错误的内容不应该再进行测试"。
据此把已并入常规流程或 GUI 已有更直观入口的关卡合并/移除：

| 原关卡 | 处置 |
| --- | --- |
| 多趟配送与中转集散（E4） | **并入**第 4 关——集散已是默认行为，不再单列 |
| 单向路与有向性（D18） | **并入**第 3 关（与网络图一起看） |
| 增量式重规划（D22） | **并入**第 8 关（与模拟路况一起看日志） |
| 图表示输出（B3） | **并入**第 3 关，且改为看 GUI 里的**表格对话框**（用户：已有 GUI 图表，无需再看文件） |
| 模拟新客户 / 道路封闭 / 手工增删 | **合并**为第 10 关（三件同类事一次做完） |
| 三种策略 + 权重标签 | **合并**为第 5 关 |

新结构 13 关：构建自动检查 → 界面布局 → 网络图/有向性/图表示 → 规划与集散 →
三策略与权重 → 拖动联动 → 超时 → 路况与增量重规划 → 紧急订单 → 动态增删 →
推进与车辆/暂存随动 → Debug → 边界不可行。

**关于"中转站提高效率"的再次核查（未实现不存在的优化）**

用户提出：车辆应"满载进入中转站""从仓库出发时顺带补充中转站"，避免空驶，
从而提高效率。我做了量化核查后**没有实现这一改动**，因为算下来它不成立：

货量从仓库跨到某簇所在区域，是任何方案都躲不掉的干线运输。每跨一次最多带走
一个载重上限 C，因此每簇的干线跨越次数下界是 `ceil(簇货量 / C)`：

    簇1 262kg → 至少 2 次；簇2 220kg → 至少 2 次；簇3 258kg → 至少 2 次

**直接送达**恰好达到这个下界；**经中转站**同样是这个下界，却额外产生
「中转站 → 配送点」的短途往返。所以中转站**不减少干线跨越次数，也就不降低总距离**。

它的真实价值仍是设计 §5.6 早已写明的三条：使超载场景可行、车辆以站为驻点
免除每趟空驶回仓库、为将来多车提供枢纽。**按用户先前的裁定，报告中不得宣称
不实收益**，故此处保持原样，并把核算过程留档。

**第 20 关"程序未能启动"**：实测无参数启动可正常存活，**无法复现**。
已加固 `launch_app`：程序输出落盘到 `/tmp/wizard_app.log`，轮询等待延长，
失败时直接把该日志打印出来——下次若再出现，即可看到真实原因。

### P12 第 9 轮裁定：中转站**不强制参与路由**（用户原则）

用户裁定（原文）：

> 中转站名字保持不变，并不是强行要经过中转站如果本来直达更快就应该去直达，
> 中转站只应该在"马上就要带着货物返回仓库"时放下这部分马上要被浪费的载荷……
> **一切决策都不应该为了满足某种策略的前提条件而去实际执行更差的策略。**

据此把 `multiTripPlan` 的中转站判定改为：

```cpp
const bool useStation = !hub.empty() && clusterTotal > vehicle.capacityKg + 1e-9
                        && stock[hub] > 1e-9;   // 只有站内**确实有存货**才考虑
```

中转站**初始无存货**，因此默认数据一律走直达分批。改动前后：

| 策略 | 改前（每簇必经站） | 改后（直达分批） |
| --- | --- | --- |
| 距离 | 198.2 km / 13 趟 / 277.2 元 / penalty 323 | **178.2 km / 6 趟 / 244.2 元 / penalty 256** |
| 成本 | 209.1 km / 267.0 元 / penalty 382 | **186.3 km / 235.9 元 / penalty 307** |
| 耗时 | 200.5 km / 281.6 元 / penalty 323 | **183.2 km / 255.2 元 / penalty 311** |

**三项策略、四项指标全面改善，趟数减半。**

`replan` 新增 `initialStock`（各站期初存货，默认为空）作为一等输入：
站里确实有货时，规划才会把该站当前置仓库使用。这一路径已有测试覆盖
（`testStationUsedOnlyWhenStockExists`：期初 0 完全绕开；期初有货则启用）。

对应地，三条旧断言（"中转站必须被使用""峰值合计 > 0"）已被**改写为守住新原则**：
**期初无存货时中转站不得参与路由**——一旦有人把"每簇必经站"加回来，它们立刻失败。

### P13 尚待实现：库存的生产者（顺路寄存）

现在 `initialStock` 已能作为输入，但**没有任何东西去填充它**——
即用户所说的"车辆即将带货运回仓库时，把马上要浪费的载荷顺路卸在中转站"。

要落地需要一个前提：**重规划时必须知道车上此刻载着什么**。
当前 `replan` 完全按 `remainingOrders()` 从零重算，**不知道车辆在途载货**，
因此中途重规划（紧急订单 / 路况 / 封路）会让车先空跑回仓库，把在途货物白带一趟。
这本身就是一个独立缺陷（见 P13 待办）。

已具备的素材：`MainWindow::currentLoadKg()` 能从"当前趟 + 推进位置"反推在途货量，
所以不必新增一份状态即可着手。

### P13 顺路寄存（前置储存点机制）——已实现

用户第 10 轮确认按**安全版**实现：只卸在**回程路径上的站**，且**必须是该站所服务簇的货**。

**关键简化**：寄存发生在规划窗口**之前**，因此不需要在任何行程里记一笔装卸，
直接体现为站里的**期初存货**即可。整个机制因此只有几十行。

`replan` 新增两个可选参数：

- `initialStock`：各站期初存货（前置储存点）
- `onboard`：车辆**此刻已载在车上**的货（`OnboardItem{nodeId, kg}`）

处理顺序（`multiTripPlan`）：

1. **先空跑一版**，得出"本次回仓库**之前**会服务哪些停靠点"；
2. 在途货里**没被这一版送掉**的部分，若其所属簇的中转站满足
   `dist(出发地,站) + dist(站,仓库) <= dist(出发地,仓库)`（即**站在回程路径上**），
   就把它并入期初存货 —— **零绕路**；不满足则原样带回去；
3. 算两版（不许用站 / 允许用站），**取更优者**，用站版 penalty 更差则一票否决。

**必须"先空跑一版"的原因**：把**全部**在途货都寄存是错的——车马上就要送掉的那些
不该卸下来。所以要按"本次回程前是否来得及送"来筛。

实测（`testOnboardSurplusIsBankedEnRoute`，载重 50kg、订单各 20kg、车在 D1 载着 D2 的货）：

```
第1趟: D1 T W                       <- 回仓库路上经过 T，顺路寄存 20kg（零绕路）
第2趟: W T D1 D2 D1 D3 D4 D3 D1 T W
T 峰值 20.0kg  期末 20.0kg          <- 存货留下，供后续使用
```

**触发条件**：不是常规直送——常规直送时车**空车**回仓库，没有可寄存的货。
真正触发的是**途中重规划**：车正在簇内执行某趟、车上载着尚未送达的货，
而新规划要求它先回仓库（例如插入远端紧急订单）。此时那部分货才"来不及送"。

**"积少成多"的回路**：GUI 用 `stationStock_` 保存最近一次规划的期末存货，
下一次重规划时作为 `initialStock` 传回（三处规划出口都会回写）。
这样寄存的货不会凭空消失，会跨重规划累积，直到被某次规划当作前置仓库用掉。

### P14 第 11 轮：两处界面呈现修正 + 向导再瘦身

**用户反馈 1：停靠明细展示混乱、等待/载货数据看起来不对。**
逐值核对后确认**数据本身无误**（D01 等待 55 分 = 窗口 09:00 − 原始到达 08:05；
送后余载 160 = 首趟装载 190 − D01 需求 30）。真正的毛病是**呈现**：
明细是**跨趟拉平**的，`剩余载重` 会从 0 跳回几十公斤、再跳到一百多，
没有趟号时看起来就像数据错了。处置：新增「**趟**」列，并给列名补上单位
（`等待(分)`、`送后余载(kg)`）。并加守卫：`--ui-probe` 断言每行首列都是「第 N」。

**用户反馈 2：完整路径上方的「第 X 趟」全是 `W01 → W01`。**
原因是该行渲染的是 `trip.nodes.front() → trip.endNodeId`；直达分批下每趟都从
仓库出发、回仓库，这两个端点**已无信息量**。处置：改为显示本趟**服务了哪些配送点**：

```
第 1 趟：7 个配送点  D01 → D04  28.2km  装载 190kg
```

**用户反馈 3：再精简向导，把"只是看看"的关卡合并。**
13 关 → **8 关**。原来"认清布局""看网络图""看规划结果""看超时表""看三策略"
是**五关纯粹的看**，熟练测试者每关都要清屏、回车、再切回程序，成本全在切换
而不在验证。合并后：

| 新关卡 | 合并了原来的 |
| --- | --- |
| 1 构建与自动检查 | 1 |
| 2 启动、界面布局、网络图与图表示 | 2 + 3 |
| 3 规划结果：路线/车辆/中转站/明细/三策略/超时 | 4 + 5 + 7 |
| 4 画布交互与手工增删健壮性 | 6 + 10(部分) |
| 5 动态事件：路况/增量重规划/紧急订单/新客户/封路 | 8 + 9 + 10(部分) |
| 6 推进一站：车辆位置、当前载重与暂存随动 | 11 |
| 7 Debug 模式 | 12 |
| 8 边界：制造不可行场景 | 13 |

每个"要不要动手"的环节仍是独立一关，不打乱节奏；纯"看"的部分全部并进相邻关。

**顺带消灭一处新的硬编码**：向导原本写死"停靠明细第一行应为 第 1 | D01 | 08:05 | ..."，
这又是一处会随数据过期的东西。已改为**现场跑 `--ui-probe` 取值展示**。

### P15 第 12 轮：修"趟号塌成 1"的显示 bug + Debug 速度卡片删除

**用户反馈（第 7 关）**：Debug 过程中停靠明细的「趟」与「等待」列显示错误
——"一直显示的趟1"，要求"修复时自己先推进几站，在动态中持续保持正确"。

**根因（我上一轮没修对的地方）**：GUI 把**整条剩余路线切成了一趟**（`restTrip`）
塞给增量式重规划。于是走增量路径后 `plan_.trips.size() == 1`，界面上的
「第 N 趟」全塌成「第 1 趟」、「共 N 趟」变成「共 1 趟」、车辆面板变成「第 1 / 1 趟」。

> 教训：上一轮我加的是"趟号列"，看着把问题修了，但**根因在更上游**——
> 剩余路线的切分方式。只在显示层打补丁，数据层已经塌了。

**修法**：把"切分尚未走完的部分"抽成 core 的 `sliceRemainder(plan, fromIndex)`，
**按趟切分、保留真实趟结构**。多趟时增量算法本就会退回全量重算（日志已说明），
此时结构自然正确；单趟时增量路径照常生效。

**守卫（三层，都经过反向验证）**：
1. `integration_tests`：走到第 3 个停靠点后切分，断言剩余部分**仍分多趟**
   （变异成"压成一趟"→ 报"实际 1 趟"失败）
2. `--ui-probe`：断言停靠明细的**趟号列真的出现多种趟号**（共 6 趟时至少 2 种）
   （变异成全塌成 1 → 报"共 6 趟，但趟号只有 1 种"退出码 1）——
   这是直接守**用户看到的那个界面**
3. 向导第 6 关：让测试者推进后回看停靠明细，核对趟号仍是 1、2、3…

**Debug 速度卡片**：按用户要求**只保留 1 秒档**、删除速度选择下拉与标签，
工具栏动作从 17 项降到 15 项。

### 明确**不做**的项（要求未提及）

多车辆调度、车辆在途位置（画在路段上）、规划结果导出/回放、
多目标加权 / 2-opt / A*。保留在 `docs/功能实现清单.md` §五 作为可选扩展记录。
